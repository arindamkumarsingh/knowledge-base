# INTRO

whenever a website is visited, the borwser which u are using sends a request to the web server to show the info of the website, then the website responds with the data shown.

*  FRONT - END = the way the browser renders the website.

* BACK - END = server - client requests are handled.

We can view the html of any website by clicking right on mouse and showing page source.

HTML(structure) + CSS (pretty) + Javascript(interactivity)


### Sensitive Info in web

Usually a web dev leaves test credentials in the source